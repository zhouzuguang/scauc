const prefix = '/api/v1'
// user
export const USERS = prefix + '/users'

// types
export const TYPES = prefix + '/types'

// catologues
export const CATOLOGUES = prefix + '/catologues'

// articles
export const ARTICLES = prefix + '/articles'

// comments
export const COMMENTS = prefix + '/comments'

// 反馈
export const FEEDBACKS = prefix + '/feedbacks'
